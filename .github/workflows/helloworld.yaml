name: "Build Test workflow"

on: workflow_dispatch

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - name: Lint Code
      run: |
        echo "Linting code"
        npm run lint
    - name: Build code
      run: |
         echo "Building code"
         npm run build
    - name: Test code
      run: |
         echo "Running tests"
         npm run test